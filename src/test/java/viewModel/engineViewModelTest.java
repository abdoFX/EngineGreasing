package viewModel;

import model.Engine;
import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;
import org.springframework.context.ApplicationContext;
import org.springframework.context.support.ClassPathXmlApplicationContext;

/**
 * Created on 27-11-2016 at 20:34.
 * Project : Engine Greasing application
 * Developper: Bouamer Abdelwaheb
 */
public class engineViewModelTest {
/*
    private ApplicationContext context = null;

    @Before
    public void setup() {
        context = new ClassPathXmlApplicationContext("/spring/config/beanLocations.xml");
    }


    @Test
    public void testSelectedItemChanged() {
        EngineViewModel engineViewModel = context.getBean("engineViewModel", EngineViewModel.class);

        Engine engine1 = engineViewModel.selectedEngineProperty().get();

        engineViewModel.selectedEngineProperty().set(new Engine());

        Assert.assertNotEquals(engine1, engineViewModel.selectedEngineProperty().get());

    }
*/
}
